/** @flow */
import styled from 'styled-components';

const Main: any = styled.main`
  margin: auto;
  max-width: 96%;
  min-height: calc(100vh - 64px);

  h1,
  ul {
    margin: 0;
  }
`;

export default Main;
